<%= form_for @parent_category, url: @parent_category.id ? admin_parent_category_path(@parent_category) : "/admin/parent_categories", :html => { :multipart => true } do |f| %>
  <% if @parent_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parent_category.errors.count, "error") %> prohibited this parent_category from being saved:</h2>

      <ul>
      <% @parent_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :display_order %>: <%= @parent_category.display_order %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
      <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :photo %><br>
    <% if @parent_category.photo.exists? %>
        <p>
          <%= image_tag @parent_category.photo.url %>
        </p>
    <% end %>
    <%= f.file_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
